<label for="species" class="control-label">
    Species
</label>
<select id="species" name="species" class="form-control" w-bind>
    <option for(item in data.species) value="(item.UUID)">${item.name}</option>
</select>
